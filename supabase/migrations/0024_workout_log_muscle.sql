-- Freeform lifts join the volume picture: interpret tags each composer/
-- Telegram-logged exercise with a muscle group (plan match first, then the
-- bundled exercise database), and the Gym tab's volume chart counts them.
-- Old rows stay null and are simply not counted - no backfill.
alter table workout_logs add column muscle_group text;
